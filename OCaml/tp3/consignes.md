Consignes du TP 3
============

Coder les fonctions suivantes :
```
dernier : 'a list -> 'a

sans_dernier : 'a list -> 'a list (liste non vide)

dernier_sans_dernier : 'a list -> 'a * 'a list (couple du dernier + liste sans le dernier)

ajout_en_fin : 'a * 'a list -> 'a list

inverse : 'a list -> 'a list (faire sans puis avec fonctions auxiliaires)

colle : 'a list * 'a list -> 'a list (fusionne deux listes, équivaut à @ en OCaml)

colle_toutes : 'a list list -> 'a list (faire avec colle puis sans utiliser de fonctions auxiliaires)
```